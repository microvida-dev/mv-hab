<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Área pessoal</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Contratos</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8"><div class="mv-surface overflow-hidden"><table class="mv-table"><thead><tr><th>Número</th><th>Habitação</th><th>Estado</th><th>Início</th><th>Fim</th><th>Renda</th><th>Caução</th><th></th></tr></thead><tbody>@forelse($contracts as $contract)<tr><td class="font-semibold">{{ $contract->contract_number }}</td><td>{{ $contract->housingUnit?->code }}</td><td>{{ $contract->status->label() }}</td><td>{{ $contract->start_date?->format('d/m/Y') }}</td><td>{{ $contract->end_date?->format('d/m/Y') }}</td><td>{{ $contract->monthly_rent }}</td><td>{{ $contract->deposit?->amount ?? '-' }}</td><td class="text-right"><a class="font-semibold text-civic-700" href="{{ route('candidate.contracts.show', $contract) }}">Abrir</a></td></tr>@empty<tr><td colspan="8" class="text-center text-ink-500">Ainda não existem contratos disponíveis.</td></tr>@endforelse</tbody></table><div class="border-t border-ink-100 px-4 py-3">{{ $contracts->links() }}</div></div></div></div>
</x-app-layout>
