<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-mvhab-primary">Contrato</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Caução</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8"><div class="mv-surface p-6">@if($leaseContract->deposit)<dl class="grid gap-4 md:grid-cols-2 text-sm"><div><dt class="text-ink-500">Valor da caução</dt><dd class="font-semibold">{{ $leaseContract->deposit->amount }} {{ $leaseContract->deposit->currency }}</dd></div><div><dt class="text-ink-500">Estado</dt><dd>{{ $leaseContract->deposit->status->label() }}</dd></div><div><dt class="text-ink-500">Data de pedido</dt><dd>{{ $leaseContract->deposit->requested_at?->format('d/m/Y H:i') ?? '-' }}</dd></div><div><dt class="text-ink-500">Data de pagamento registado</dt><dd>{{ $leaseContract->deposit->paid_at?->format('d/m/Y H:i') ?? '-' }}</dd></div><div class="md:col-span-2"><dt class="text-ink-500">Observações</dt><dd>{{ $leaseContract->deposit->notes ?? '-' }}</dd></div></dl>@else<p class="text-sm text-ink-500">Sem caução registada para este contrato.</p>@endif</div></div></div>
</x-app-layout>
