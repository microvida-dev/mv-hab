<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-mvhab-primary">Contrato</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $leaseContract->contract_number }}</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-5xl space-y-5 px-4 sm:px-6 lg:px-8"><div class="mv-surface p-6"><p class="text-sm text-ink-600">O contrato fica disponível para consulta após emissão pelos serviços municipais. Os documentos apresentados nesta área correspondem à versão registada no sistema.</p><dl class="mt-5 grid gap-4 md:grid-cols-2 text-sm"><div><dt class="text-ink-500">Estado</dt><dd class="font-semibold">{{ $leaseContract->status->label() }}</dd></div><div><dt class="text-ink-500">Habitação</dt><dd>{{ $leaseContract->housing_address }}</dd></div><div><dt class="text-ink-500">Renda</dt><dd>{{ $leaseContract->monthly_rent }}</dd></div><div><dt class="text-ink-500">Caução</dt><dd>{{ $leaseContract->deposit?->amount ?? '-' }}</dd></div><div><dt class="text-ink-500">Início</dt><dd>{{ $leaseContract->start_date?->format('d/m/Y') }}</dd></div><div><dt class="text-ink-500">Fim</dt><dd>{{ $leaseContract->end_date?->format('d/m/Y') }}</dd></div></dl><div class="mt-5"><a class="mv-button-secondary" href="{{ route('candidate.contracts.deposit.show', $leaseContract) }}">Ver caução</a></div></div><div class="mv-surface p-6"><h2 class="font-semibold">Documentos disponíveis</h2><ul class="mt-3 space-y-2 text-sm">@forelse($leaseContract->generatedDocuments as $document)<li><a class="font-semibold text-mvhab-primary" href="{{ route('candidate.contracts.documents.download', $document) }}">{{ $document->title }}</a> · {{ $document->status->label() }}</li>@empty<li class="text-ink-500">Sem documentos emitidos.</li>@endforelse</ul></div></div></div>
</x-app-layout>
