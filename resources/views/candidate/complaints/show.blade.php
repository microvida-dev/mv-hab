<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Reclamação</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $complaint->complaint_number }}</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-4xl space-y-6 px-4 sm:px-6 lg:px-8"><div class="rounded-md border border-ink-100 bg-white p-6"><p class="text-sm text-ink-500">{{ $complaint->status->label() }}</p><h2 class="mt-4 font-semibold">{{ $complaint->subject }}</h2><p class="mt-2 whitespace-pre-line text-sm">{{ $complaint->grounds }}</p></div>@if($complaint->status->value === 'draft')<form method="POST" action="{{ route('candidate.complaints.submit', $complaint) }}">@csrf<label class="mb-3 flex items-center gap-2 text-sm"><input type="checkbox" name="truthfulness_confirmed" value="1" required> Confirmo a veracidade dos elementos apresentados.</label><x-primary-button>Submeter reclamação</x-primary-button></form>@endif @if($complaint->decision && $complaint->decision->candidate_visible)<div class="rounded-md border border-ink-100 bg-white p-6"><h2 class="font-semibold">Decisão</h2><p class="mt-2 text-sm">{{ $complaint->decision->summary }}</p></div>@endif</div></div>
</x-app-layout>

