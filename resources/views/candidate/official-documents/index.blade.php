<x-app-layout><x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Área pessoal</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Documentos oficiais</h1></div></x-slot><div class="py-8"><div class="mx-auto max-w-5xl space-y-4 px-4 sm:px-6 lg:px-8">@forelse($documents as $document)<a href="{{ route('candidate.official-documents.show', $document) }}" class="block rounded-md border border-ink-100 bg-white p-5"><div class="flex flex-wrap justify-between gap-3"><div><p class="font-semibold">{{ $document->title }}</p><p class="mt-1 font-mono text-xs text-ink-500">{{ $document->document_number }}</p></div><div class="text-right"><p class="text-sm">{{ $document->status->label() }}</p><p class="mt-1 text-xs text-ink-500">{{ $document->generated_at?->format('d/m/Y') }}</p></div></div></a>@empty<div class="rounded-md border border-ink-100 bg-white p-10 text-center text-sm text-ink-500">Sem documentos oficiais disponíveis.</div>@endforelse{{ $documents->links() }}</div></div></x-app-layout>
