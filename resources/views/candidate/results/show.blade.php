<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Resultado</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $provisionalList->title }}</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-4xl space-y-6 px-4 sm:px-6 lg:px-8"><div class="rounded-md border border-ink-100 bg-white p-6"><dl class="grid gap-4 md:grid-cols-2 text-sm"><div><dt class="text-ink-500">Número público</dt><dd class="font-semibold">{{ $entry->public_identifier }}</dd></div><div><dt class="text-ink-500">Estado</dt><dd>{{ $entry->status->label() }}</dd></div><div><dt class="text-ink-500">Posição</dt><dd>{{ $entry->rank_position ?? 'Não aplicável' }}</dd></div><div><dt class="text-ink-500">Prazo de reclamação</dt><dd>{{ $provisionalList->complaint_period_ends_at?->format('d/m/Y H:i') }}</dd></div></dl>@if($entry->exclusion_reason)<p class="mt-4 text-sm">{{ $entry->exclusion_reason }}</p>@endif</div>@if($provisionalList->isComplaintPeriodOpen())<a href="{{ route('candidate.complaints.create', ['provisional_list_entry_id' => $entry->id]) }}" class="inline-block rounded-md bg-civic-700 px-4 py-2 text-sm font-semibold text-white">Apresentar reclamação</a>@endif @if($definitiveList)<p class="rounded-md bg-civic-50 p-4 text-sm text-civic-900">A lista definitiva encontra-se disponível. Esta lista resulta da análise das candidaturas, reclamações e demais atos procedimentais aplicáveis.</p>@endif</div></div>
</x-app-layout>

