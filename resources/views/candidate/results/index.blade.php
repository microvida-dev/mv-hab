<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Área do candidato</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Resultados</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-5xl space-y-6 px-4 sm:px-6 lg:px-8"><div class="rounded-md border border-ink-100 bg-white p-6"><h2 class="font-semibold">Listas provisórias</h2>@forelse($provisionalEntries as $entry)<a href="{{ route('candidate.results.show', $entry->provisionalList) }}" class="mt-3 block rounded-md border border-ink-100 p-4"><span class="font-semibold">{{ $entry->provisionalList->contest?->title }}</span><span class="ml-2 text-sm text-ink-500">{{ $entry->public_identifier }} · {{ $entry->status->label() }}</span></a>@empty<p class="mt-2 text-sm text-ink-500">Sem resultados provisórios publicados.</p>@endforelse</div><div class="rounded-md border border-ink-100 bg-white p-6"><h2 class="font-semibold">Listas definitivas</h2>@forelse($definitiveEntries as $entry)<p class="mt-3 rounded-md border border-ink-100 p-4">{{ $entry->definitiveList->contest?->title }} · {{ $entry->public_identifier }} · {{ $entry->status->label() }}</p>@empty<p class="mt-2 text-sm text-ink-500">Sem listas definitivas publicadas.</p>@endforelse</div></div></div>
</x-app-layout>

