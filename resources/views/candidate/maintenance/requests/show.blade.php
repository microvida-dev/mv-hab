<x-app-layout>
    <x-slot name="header"><h1 class="text-xl font-semibold text-ink-900">{{ $maintenanceRequest->request_number }}</h1></x-slot>
    <div class="space-y-6"><div class="mv-card grid gap-3 md:grid-cols-3"><div><p class="text-xs text-ink-500">Estado</p><p class="font-semibold">{{ $maintenanceRequest->status->label() }}</p></div><div><p class="text-xs text-ink-500">Categoria</p><p class="font-semibold">{{ $maintenanceRequest->category?->name ?? '-' }}</p></div><div><p class="text-xs text-ink-500">Urgência</p><p class="font-semibold">{{ $maintenanceRequest->urgency?->label() }}</p></div></div><div class="mv-card"><h2 class="font-semibold">{{ $maintenanceRequest->title }}</h2><p class="mt-2">{{ $maintenanceRequest->description }}</p>@if($maintenanceRequest->resolution_summary)<p class="mt-3 text-sm text-ink-600">{{ $maintenanceRequest->resolution_summary }}</p>@endif @if($maintenanceRequest->rejection_reason)<p class="mt-3 text-sm text-ink-600">{{ $maintenanceRequest->rejection_reason }}</p>@endif</div><div class="mv-card"><h2 class="font-semibold">Histórico</h2>@foreach ($maintenanceRequest->statusHistories as $history)<p class="mt-2 text-sm">{{ $history->changed_at?->format('d/m/Y H:i') }} · {{ $history->to_status?->label() }}</p>@endforeach</div></div>
</x-app-layout>
