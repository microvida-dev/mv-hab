<x-app-layout><x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Área pessoal</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Histórico de comunicações</h1></div></x-slot><div class="py-8"><div class="mx-auto max-w-5xl space-y-4 px-4 sm:px-6 lg:px-8">@forelse($communications as $communication)<a href="{{ route('candidate.communications.show', $communication) }}" class="block rounded-md border border-ink-100 bg-white p-5"><div class="flex flex-wrap justify-between gap-3"><div><p class="font-semibold">{{ $communication->title }}</p><p class="mt-1 font-mono text-xs text-ink-500">{{ $communication->communication_number }}</p></div><div class="text-right"><p class="text-sm">{{ $communication->status->label() }}</p><p class="mt-1 text-xs text-ink-500">{{ $communication->created_at->format('d/m/Y H:i') }}</p></div></div></a>@empty<div class="rounded-md border border-ink-100 bg-white p-10 text-center text-sm text-ink-500">Sem comunicações.</div>@endforelse{{ $communications->links() }}</div></div></x-app-layout>
