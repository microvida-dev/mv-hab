<x-app-layout><x-slot name="header"><div><p class="font-mono text-xs text-civic-700">{{ $communicationLog->communication_number }}</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $communicationLog->title }}</h1></div></x-slot><div class="py-8"><div class="mx-auto max-w-4xl space-y-6 px-4 sm:px-6 lg:px-8"><article class="rounded-md border border-ink-100 bg-white p-7"><p class="text-sm text-ink-500">{{ $communicationLog->status->label() }} · {{ $communicationLog->created_at->format('d/m/Y H:i') }}</p><p class="mt-6 whitespace-pre-line text-sm leading-7">{{ $communicationLog->body_snapshot }}</p></article><section><h2 class="text-lg font-semibold">Estado por canal</h2><div class="mt-3 space-y-2">@foreach($communicationLog->deliveries as $delivery)<p class="rounded-md border border-ink-100 bg-white px-4 py-3 text-sm">{{ $delivery->channel->label() }} · {{ $delivery->status->label() }}</p>@endforeach</div></section>@if($communicationLog->receipts->isNotEmpty())<section><h2 class="text-lg font-semibold">Comprovativos</h2><p class="mt-2 text-sm text-ink-500">Os comprovativos associados a notificações internas ficam disponíveis na própria notificação.</p></section>@endif</div></div></x-app-layout>
