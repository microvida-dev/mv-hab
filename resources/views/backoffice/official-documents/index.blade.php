<x-app-layout><x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Documentos oficiais</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Documentos gerados</h1></div></x-slot><div class="py-8"><div class="mx-auto max-w-7xl space-y-8 px-4 sm:px-6 lg:px-8">
    @can('create', \App\Models\GeneratedOfficialDocument::class)<form method="POST" action="{{ route('backoffice.official-documents.generate') }}" class="grid gap-4 rounded-md border border-ink-100 bg-white p-6 md:grid-cols-3">@csrf<select name="document_template_id" class="rounded-md border-ink-200" required><option value="">Modelo documental</option>@foreach($templates as $template)<option value="{{ $template->id }}">{{ $template->name }}</option>@endforeach</select><select name="recipient_user_id" class="rounded-md border-ink-200"><option value="">Sem destinatário associado</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }}</option>@endforeach</select><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="issue_immediately" value="1" class="rounded border-ink-300 text-civic-700">Emitir imediatamente</label><x-text-input name="variables[recipient_name]" placeholder="Nome autorizado do destinatário" /><x-text-input name="variables[event_reference]" placeholder="Referência do evento/processo" /><x-text-input name="variables[deadline]" placeholder="Prazo aplicável" /><x-text-input name="variables[municipality_name]" placeholder="Município" /><p class="text-xs text-ink-500">Preencha apenas dados autorizados para a finalidade do documento.</p><div class="text-right"><x-primary-button>Gerar documento</x-primary-button></div></form>@endcan
    <div class="overflow-hidden rounded-md border border-ink-100 bg-white"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Número</th><th class="px-4 py-3">Título</th><th class="px-4 py-3">Destinatário</th><th class="px-4 py-3">Estado</th><th></th></tr></thead><tbody class="divide-y divide-ink-100">@forelse($documents as $document)<tr><td class="px-4 py-3 font-mono text-xs">{{ $document->document_number }}</td><td class="px-4 py-3 font-semibold">{{ $document->title }}</td><td class="px-4 py-3">{{ $document->recipient?->name ?? 'Não associado' }}</td><td class="px-4 py-3">{{ $document->status->label() }}</td><td class="px-4 py-3 text-right"><a href="{{ route('backoffice.official-documents.show', $document) }}" class="font-semibold text-civic-700">Abrir</a></td></tr>@empty<tr><td colspan="5" class="px-4 py-10 text-center text-ink-500">Sem documentos gerados.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $documents->links() }}</div>
</div></div></x-app-layout>
