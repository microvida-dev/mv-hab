<x-app-layout><x-slot name="header"><div><p class="font-mono text-xs text-civic-700">{{ $generatedOfficialDocument->document_number }}</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $generatedOfficialDocument->title }}</h1></div></x-slot><div class="py-8"><div class="mx-auto max-w-5xl space-y-6 px-4 sm:px-6 lg:px-8"><section class="grid gap-5 border-b border-ink-100 pb-6 md:grid-cols-3"><div><p class="text-xs font-semibold uppercase text-ink-500">Estado</p><p class="mt-2">{{ $generatedOfficialDocument->status->label() }}</p></div><div><p class="text-xs font-semibold uppercase text-ink-500">Destinatário</p><p class="mt-2">{{ $generatedOfficialDocument->recipient?->name ?? 'Não associado' }}</p></div><div><p class="text-xs font-semibold uppercase text-ink-500">Gerado em</p><p class="mt-2">{{ $generatedOfficialDocument->generated_at?->format('d/m/Y H:i') }}</p></div></section><div class="flex flex-wrap gap-3"><a href="{{ route('backoffice.official-documents.download', $generatedOfficialDocument) }}" class="mv-button-primary">Descarregar HTML</a>@can('update', $generatedOfficialDocument)<form method="POST" action="{{ route('backoffice.official-documents.issue', $generatedOfficialDocument) }}">@csrf<x-secondary-button>Emitir</x-secondary-button></form><form method="POST" action="{{ route('backoffice.official-documents.cancel', $generatedOfficialDocument) }}" class="flex gap-2">@csrf<x-text-input name="cancellation_reason" placeholder="Motivo do cancelamento" required /><x-danger-button>Cancelar</x-danger-button></form>@endcan</div><iframe sandbox title="Pré-visualização do documento" srcdoc="{{ $generatedOfficialDocument->html_content }}" class="h-[700px] w-full rounded-md border border-ink-100 bg-white"></iframe></div></div></x-app-layout>
