<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-start justify-between gap-4"><div><p class="text-sm font-semibold text-civic-700">Ranking interno</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Snapshot #{{ $snapshot->snapshot_number }}</h1><p class="mt-1 text-sm text-ink-500">{{ $snapshot->contest?->title ?? $snapshot->program?->name }}</p></div><div class="flex gap-2">@can('lock', $snapshot)<form method="POST" action="{{ route('backoffice.scoring.ranking-snapshots.lock', $snapshot) }}">@csrf<button class="rounded-md border border-ink-200 px-3 py-2 text-sm font-semibold text-ink-700">Bloquear</button></form>@endcan @can('archive', $snapshot)<form method="POST" action="{{ route('backoffice.scoring.ranking-snapshots.archive', $snapshot) }}">@csrf<button class="rounded-md border border-ink-200 px-3 py-2 text-sm font-semibold text-ink-700">Arquivar</button></form>@endcan</div></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="rounded-md border border-ink-100 bg-white p-6"><p class="mb-4 text-sm text-ink-600">Ranking interno não publicado. A exposição ao candidato e listas provisórias ficam fora da Sprint 10.</p><div class="overflow-x-auto"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Posição</th><th class="px-4 py-3">Candidatura</th><th class="px-4 py-3">Candidato</th><th class="px-4 py-3">Pontuação</th><th class="px-4 py-3">Desempates</th><th class="px-4 py-3">Estado</th></tr></thead><tbody class="divide-y divide-ink-100">@foreach ($snapshot->entries as $entry)<tr><td class="px-4 py-3 font-semibold">{{ $entry->rank_position ?? '-' }}</td><td class="px-4 py-3">{{ $entry->application?->application_number }}</td><td class="px-4 py-3 text-ink-600">{{ $entry->application?->user?->name }}</td><td class="px-4 py-3 text-ink-600">{{ $entry->total_score }}</td><td class="px-4 py-3 text-ink-600">{{ collect($entry->tie_breaker_values ?? [])->pluck('value', 'code')->map(fn($v, $k) => $k.': '.$v)->implode('; ') ?: '-' }}</td><td class="px-4 py-3 text-ink-600">{{ $entry->status->label() }}</td></tr>@endforeach</tbody></table></div></div></div></div>
</x-app-layout>
