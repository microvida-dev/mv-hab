<x-app-layout>
    <x-slot name="header">
        <div class="flex flex-wrap items-start justify-between gap-4"><div><p class="text-sm font-semibold text-mvhab-primary">Execução #{{ $run->id }}</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $run->ruleSet?->name }}</h1><p class="mt-1 text-sm text-ink-500">{{ $run->contest?->title ?? $run->program?->name ?? 'Sem contexto' }}</p></div>@can('lock', $run)<form method="POST" action="{{ route('backoffice.scoring.runs.lock', $run) }}">@csrf<button class="mv-button-secondary">Bloquear</button></form>@endcan</div>
    </x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-6 px-4 sm:px-6 lg:px-8">
        <div class="grid gap-4 md:grid-cols-4">
            <div class="mv-surface p-4"><p class="text-sm text-ink-500">Estado</p><p class="mt-1 font-semibold">{{ $run->status->label() }}</p></div>
            <div class="mv-surface p-4"><p class="text-sm text-ink-500">Total admitidas</p><p class="mt-1 font-semibold">{{ $run->total_applications }}</p></div>
            <div class="mv-surface p-4"><p class="text-sm text-ink-500">Pontuadas</p><p class="mt-1 font-semibold">{{ $run->scored_applications }}</p></div>
            <div class="mv-surface p-4"><p class="text-sm text-ink-500">Avaliação manual</p><p class="mt-1 font-semibold">{{ $run->manual_review_applications }}</p></div>
        </div>
        <div class="mv-surface p-6"><h2 class="text-lg font-semibold text-ink-900">Pontuações</h2><div class="mt-4 overflow-x-auto"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Candidatura</th><th class="px-4 py-3">Candidato</th><th class="px-4 py-3">Total</th><th class="px-4 py-3">Ranking</th><th class="px-4 py-3">Estado</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-ink-100">@foreach ($run->applicationScores as $score)<tr><td class="px-4 py-3 font-semibold">{{ $score->application?->application_number }}</td><td class="px-4 py-3 text-ink-600">{{ $score->application?->user?->name }}</td><td class="px-4 py-3 text-ink-600">{{ $score->total_score }}</td><td class="px-4 py-3 text-ink-600">{{ $score->rank_position ?? '-' }}</td><td class="px-4 py-3 text-ink-600">{{ $score->status->label() }}</td><td class="px-4 py-3 text-right"><a href="{{ route('backoffice.scoring.application-scores.show', $score) }}" class="font-semibold text-mvhab-primary">Detalhe</a></td></tr>@endforeach</tbody></table></div></div>
        <div class="mv-surface p-6"><h2 class="text-lg font-semibold text-ink-900">Snapshots internos</h2><div class="mt-4 space-y-2">@forelse ($run->rankingSnapshots as $snapshot)<a href="{{ route('backoffice.scoring.ranking-snapshots.show', $snapshot) }}" class="block rounded-2xl border border-ink-100 px-4 py-3 text-sm font-semibold text-mvhab-primary">Snapshot #{{ $snapshot->snapshot_number }} - {{ $snapshot->status->label() }}</a>@empty<p class="text-sm text-ink-500">Sem snapshots.</p>@endforelse</div></div>
    </div></div>
</x-app-layout>
