<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-start justify-between gap-4"><div><p class="text-sm font-semibold text-mvhab-primary">Pontuação</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $score->application?->application_number }}</h1><p class="mt-1 text-sm text-ink-500">{{ $score->application?->user?->name }} - {{ $score->contest?->title }}</p></div><div class="flex gap-2">@can('manualReview', $score)<a href="{{ route('backoffice.scoring.application-scores.manual-review', $score) }}" class="mv-button-primary">Avaliação manual</a>@endcan @can('lock', $score)<form method="POST" action="{{ route('backoffice.scoring.application-scores.lock', $score) }}">@csrf<button class="mv-button-secondary">Bloquear</button></form>@endcan</div></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-6 px-4 sm:px-6 lg:px-8">
        <div class="grid gap-4 md:grid-cols-5"><div class="mv-surface p-4"><p class="text-sm text-ink-500">Automática</p><p class="font-semibold">{{ $score->automatic_score }}</p></div><div class="mv-surface p-4"><p class="text-sm text-ink-500">Manual</p><p class="font-semibold">{{ $score->manual_score }}</p></div><div class="mv-surface p-4"><p class="text-sm text-ink-500">Total</p><p class="font-semibold">{{ $score->total_score }}</p></div><div class="mv-surface p-4"><p class="text-sm text-ink-500">Posição</p><p class="font-semibold">{{ $score->rank_position ?? '-' }}</p></div><div class="mv-surface p-4"><p class="text-sm text-ink-500">Estado</p><p class="font-semibold">{{ $score->status->label() }}</p></div></div>
        <div class="mv-surface p-6"><h2 class="text-lg font-semibold text-ink-900">Detalhe por critério</h2><div class="mt-4 overflow-x-auto"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Código</th><th class="px-4 py-3">Nome</th><th class="px-4 py-3">Resultado</th><th class="px-4 py-3">Pontos</th><th class="px-4 py-3">Manual</th><th class="px-4 py-3">Mensagem</th></tr></thead><tbody class="divide-y divide-ink-100">@foreach ($score->details as $detail)<tr><td class="px-4 py-3 font-mono text-xs">{{ $detail->code }}</td><td class="px-4 py-3 font-semibold">{{ $detail->name }}</td><td class="px-4 py-3 text-ink-600">{{ $detail->result->label() }}</td><td class="px-4 py-3 text-ink-600">{{ $detail->points_awarded }} / {{ $detail->max_points ?? '-' }}</td><td class="px-4 py-3 text-ink-600">{{ $detail->requires_manual_review ? ($detail->reviewed_at ? 'Revisto' : 'Pendente') : 'Não' }}</td><td class="px-4 py-3 text-ink-600">{{ $detail->message }}</td></tr>@endforeach</tbody></table></div></div>
    </div></div>
</x-app-layout>
