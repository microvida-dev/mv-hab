<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-4"><div><p class="text-sm font-semibold text-mvhab-primary">{{ $ruleSet->name }}</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Regras de desempate</h1></div>@can('create', [\App\Models\TieBreakerRule::class, $ruleSet])<a href="{{ route('backoffice.scoring.tie-breakers.create', $ruleSet) }}" class="mv-button-primary">Nova regra</a>@endcan</div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8"><div class="mv-surface overflow-hidden"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Código</th><th class="px-4 py-3">Nome</th><th class="px-4 py-3">Alvo</th><th class="px-4 py-3">Direção</th><th class="px-4 py-3">Prioridade</th><th class="px-4 py-3"></th></tr></thead><tbody class="divide-y divide-ink-100">@forelse ($rules as $rule)<tr><td class="px-4 py-3 font-mono text-xs">{{ $rule->code }}</td><td class="px-4 py-3 font-semibold">{{ $rule->name }}</td><td class="px-4 py-3 text-ink-600">{{ $rule->target }}</td><td class="px-4 py-3 text-ink-600">{{ $rule->direction->label() }}</td><td class="px-4 py-3 text-ink-600">{{ $rule->priority_order }}</td><td class="px-4 py-3 text-right"><a href="{{ route('backoffice.scoring.tie-breakers.edit', $rule) }}" class="font-semibold text-mvhab-primary">Editar</a></td></tr>@empty<tr><td colspan="6" class="px-4 py-8 text-center text-ink-500">Sem regras de desempate.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $rules->links() }}</div></div></div>
</x-app-layout>
