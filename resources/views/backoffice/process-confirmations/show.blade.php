<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-start justify-between gap-4"><div><p class="text-sm font-semibold text-civic-700">Confirmação de processo</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $processConfirmation->process_number }}</h1><p class="mt-1 text-sm text-ink-500">{{ $processConfirmation->confirmation_number }} · {{ $processConfirmation->status->label() }}</p></div><form method="POST" action="{{ route('backoffice.process-confirmations.send', $processConfirmation) }}">@csrf <button class="mv-button-primary">Marcar como enviada</button></form></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-4xl space-y-6 px-4 sm:px-6 lg:px-8">@if (session('success'))<div class="rounded-md border border-civic-200 bg-civic-50 px-4 py-3 text-sm font-semibold text-civic-800">{{ session('success') }}</div>@endif<section class="mv-surface p-6"><h2 class="text-lg font-semibold text-ink-900">{{ $processConfirmation->title }}</h2><p class="mt-4 text-sm leading-6 text-ink-700">{{ $processConfirmation->message }}</p>@if($processConfirmation->application)<a class="mt-4 inline-block font-semibold text-civic-700" href="{{ route('backoffice.applications.show', $processConfirmation->application) }}">Abrir candidatura</a>@endif</section><section class="mv-surface p-6"><h2 class="text-lg font-semibold text-ink-900">Dados preservados</h2><pre class="mt-4 overflow-auto rounded-md bg-ink-50 p-4 text-xs">{{ json_encode($processConfirmation->payload, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE) }}</pre></section></div></div>
</x-app-layout>
