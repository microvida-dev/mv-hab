<x-app-layout>
    <x-slot name="header"><h1 class="text-xl font-semibold text-ink-900">Revisão #{{ $rentReview->id }}</h1></x-slot>
    <div class="mv-card space-y-4"><p>Estado: <strong>{{ $rentReview->status->label() }}</strong></p><p>Renda atual: {{ number_format((float) $rentReview->current_rent, 2, ',', '.') }} EUR</p><form method="POST" action="{{ route('backoffice.finance.rent-reviews.calculate', $rentReview) }}" class="flex gap-2">@csrf<input class="mv-input" name="proposed_rent" type="number" step="0.01" placeholder="Proposta"><button class="mv-button-secondary">Calcular</button></form><form method="POST" action="{{ route('backoffice.finance.rent-reviews.approve', $rentReview) }}" class="flex gap-2">@csrf<input class="mv-input" name="approved_rent" type="number" step="0.01" value="{{ $rentReview->proposed_rent }}"><button class="mv-button-secondary">Aprovar</button></form><form method="POST" action="{{ route('backoffice.finance.rent-reviews.apply', $rentReview) }}">@csrf<button class="mv-button-primary">Aplicar</button></form></div>
</x-app-layout>
