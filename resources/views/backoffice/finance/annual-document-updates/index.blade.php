<x-app-layout>
    <x-slot name="header"><h1 class="text-xl font-semibold text-ink-900">Atualização documental anual</h1></x-slot>
    <form method="POST" action="{{ route('backoffice.finance.annual-document-updates.store') }}" class="mv-card mb-4 grid gap-3 md:grid-cols-[1fr_160px_auto]">@csrf<input class="mv-input" name="tenant_financial_account_id" placeholder="ID da conta financeira" required><input class="mv-input" name="reference_year" type="number" value="{{ now()->year }}"><button class="mv-button-primary">Solicitar</button></form>
    <div class="mv-card overflow-x-auto"><table class="min-w-full text-sm"><tbody>@foreach ($requests as $request)<tr class="border-t border-ink-100"><td class="py-2"><a class="text-civic-700" href="{{ route('backoffice.finance.annual-document-updates.show', $request) }}">{{ $request->request_number }}</a></td><td>{{ $request->tenant?->name }}</td><td>{{ $request->status->label() }}</td></tr>@endforeach</tbody></table>{{ $requests->links() }}</div>
</x-app-layout>
