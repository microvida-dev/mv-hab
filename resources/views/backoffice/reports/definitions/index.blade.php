<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between gap-4"><h1 class="text-xl font-semibold text-ink-900">Definições de relatório</h1>@can('create', \App\Models\ReportDefinition::class)<a class="mv-button-primary" href="{{ route('backoffice.reports.definitions.create') }}">Nova definição</a>@endcan</div></x-slot>
    <div class="overflow-x-auto"><table class="mv-table"><thead><tr><th>Nome</th><th>Tipo</th><th>Sensibilidade</th><th>Estado</th></tr></thead><tbody>@foreach ($reports as $report)<tr><td><a class="font-semibold text-mvhab-primary" href="{{ route('backoffice.reports.definitions.show', $report) }}">{{ $report->name }}</a></td><td>{{ $report->report_type->label() }}</td><td>{{ $report->sensitivity_level->label() }}</td><td>{{ $report->is_active ? 'Ativo' : 'Inativo' }}</td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $reports->links() }}</div>
</x-app-layout>
