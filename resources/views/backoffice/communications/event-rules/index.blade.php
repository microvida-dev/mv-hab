<x-app-layout>
    <x-slot name="header"><div class="flex items-end justify-between"><div><p class="text-sm font-semibold text-civic-700">Automação</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Regras de comunicação por evento</h1></div>@can('create', \App\Models\NotificationEventRule::class)<a href="{{ route('backoffice.communications.event-rules.create') }}" class="mv-button-primary">Nova regra</a>@endcan</div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="overflow-hidden rounded-md border border-ink-100 bg-white"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Evento</th><th class="px-4 py-3">Destinatário</th><th class="px-4 py-3">Canal</th><th class="px-4 py-3">Template</th><th class="px-4 py-3">Estado</th><th></th></tr></thead><tbody class="divide-y divide-ink-100">
        @forelse($rules as $rule)<tr><td class="px-4 py-3"><p class="font-semibold">{{ $rule->name }}</p><p class="font-mono text-xs text-ink-500">{{ $rule->event_code }}</p></td><td class="px-4 py-3">{{ $rule->recipient_type }}</td><td class="px-4 py-3">{{ $rule->channel->label() }}</td><td class="px-4 py-3">{{ $rule->template?->name }}</td><td class="px-4 py-3">{{ $rule->is_active ? 'Ativa' : 'Inativa' }}</td><td class="px-4 py-3"><div class="flex justify-end gap-2"><a class="font-semibold text-civic-700" href="{{ route('backoffice.communications.event-rules.edit', $rule) }}">Editar</a>@can('update', $rule)<form method="POST" action="{{ route($rule->is_active ? 'backoffice.communications.event-rules.deactivate' : 'backoffice.communications.event-rules.activate', $rule) }}">@csrf<button class="font-semibold text-ink-600">{{ $rule->is_active ? 'Desativar' : 'Ativar' }}</button></form>@endcan</div></td></tr>@empty<tr><td colspan="6" class="px-4 py-10 text-center text-ink-500">Sem regras configuradas.</td></tr>@endforelse
        </tbody></table></div><div class="mt-4">{{ $rules->links() }}</div></div></div>
</x-app-layout>
