<x-app-layout>
    <x-slot name="header"><div><p class="font-mono text-xs text-mvhab-primary">{{ $communicationLog->communication_number }}</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $communicationLog->title }}</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-6xl space-y-8 px-4 sm:px-6 lg:px-8">
        <section class="grid gap-6 border-b border-ink-100 pb-8 md:grid-cols-4"><div><p class="text-xs font-semibold uppercase text-ink-500">Estado</p><p class="mt-2">{{ $communicationLog->status->label() }}</p></div><div><p class="text-xs font-semibold uppercase text-ink-500">Destinatário</p><p class="mt-2">{{ $communicationLog->recipient?->name }}</p></div><div><p class="text-xs font-semibold uppercase text-ink-500">Evento</p><p class="mt-2">{{ $communicationLog->event_code }}</p></div><div><p class="text-xs font-semibold uppercase text-ink-500">Prioridade</p><p class="mt-2">{{ $communicationLog->priority->label() }}</p></div></section>
        @can('update', $communicationLog)<div class="flex flex-wrap gap-3"><form method="POST" action="{{ route('backoffice.communications.logs.cancel', $communicationLog) }}">@csrf<x-danger-button>Cancelar pendente</x-danger-button></form><form method="POST" action="{{ route('backoffice.communications.logs.archive', $communicationLog) }}">@csrf<x-secondary-button>Arquivar</x-secondary-button></form></div>@endcan
        <section><h2 class="text-lg font-semibold">Conteúdo imutável enviado</h2><p class="mt-4 whitespace-pre-line text-sm leading-7 text-ink-700">{{ $communicationLog->body_snapshot }}</p></section>
        <section><h2 class="text-lg font-semibold">Entregas e tentativas</h2><div class="mt-4 space-y-4">@forelse($communicationLog->deliveries as $delivery)<div class="rounded-2xl mv-surface p-5"><div class="flex flex-wrap items-center justify-between gap-3"><p class="font-semibold">{{ $delivery->channel->label() }} · {{ $delivery->status->label() }}</p>@can('update', $delivery)<form method="POST" action="{{ route('backoffice.communications.deliveries.resend', $delivery) }}">@csrf<x-secondary-button>Reenviar</x-secondary-button></form>@endcan</div><p class="mt-2 text-sm text-ink-500">{{ $delivery->destination ?: 'Sem destino externo' }}</p><div class="mt-4 space-y-2">@foreach($delivery->attempts as $attempt)<p class="text-xs text-ink-600">Tentativa {{ $attempt->attempt_number }} · {{ $attempt->status->label() }}{{ $attempt->error_message ? ' · '.$attempt->error_message : '' }}</p>@endforeach</div>@if($delivery->channel === \App\Enums\CommunicationChannel::Postal && !$delivery->sent_at)@can('update', $delivery)<form method="POST" action="{{ route('backoffice.communications.deliveries.postal', $delivery) }}" enctype="multipart/form-data" class="mt-5 grid gap-3 md:grid-cols-3">@csrf<x-text-input type="datetime-local" name="sent_at" required /><x-text-input name="postal_reference" placeholder="Referência postal" /><input type="file" name="receipt_file" class="text-sm"><div class="md:col-span-3"><x-primary-button>Registar envio postal</x-primary-button></div></form>@endcan@endif</div>@empty<p class="text-sm text-ink-500">Sem entregas.</p>@endforelse</div></section>
        @if($communicationLog->receipts->isNotEmpty())<section><h2 class="text-lg font-semibold">Comprovativos</h2><div class="mt-4 flex flex-wrap gap-3">@foreach($communicationLog->receipts as $receipt)<a class="mv-button-secondary" href="{{ route('backoffice.communications.receipts.download', $receipt) }}">Descarregar {{ $receipt->receipt_number }}</a>@endforeach</div></section>@endif
    </div></div>
</x-app-layout>
