<x-app-layout><x-slot name="header"><div><p class="text-sm font-semibold text-mvhab-primary">RGPD e canais</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Preferências de notificação</h1></div></x-slot><div class="py-8"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="overflow-hidden rounded-2xl mv-surface"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Utilizador</th><th class="px-4 py-3">In-app</th><th class="px-4 py-3">Email</th><th class="px-4 py-3">SMS</th><th class="px-4 py-3">Postal</th><th class="px-4 py-3">Preferido</th></tr></thead><tbody class="divide-y divide-ink-100">@forelse($preferences as $preference)<tr><td class="px-4 py-3 font-semibold">{{ $preference->user?->name }}</td><td class="px-4 py-3">{{ $preference->allow_in_app ? 'Ativo' : 'Inativo' }}</td><td class="px-4 py-3">{{ $preference->allow_email ? 'Ativo' : 'Inativo' }}</td><td class="px-4 py-3">{{ $preference->allow_sms ? 'Ativo' : 'Inativo' }}</td><td class="px-4 py-3">{{ $preference->allow_postal ? 'Ativo' : 'Inativo' }}</td><td class="px-4 py-3">{{ $preference->preferred_channel?->label() ?? 'Não definido' }}</td></tr>@empty<tr><td colspan="6" class="px-4 py-10 text-center text-ink-500">Sem preferências registadas.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $preferences->links() }}</div></div></div></x-app-layout>
