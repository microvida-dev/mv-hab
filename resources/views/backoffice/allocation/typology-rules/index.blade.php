<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between gap-4"><div><p class="text-sm font-semibold text-mvhab-primary">Atribuição</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Regras de adequação</h1></div><a href="{{ route('backoffice.allocation.typology-rules.create') }}" class="mv-button-primary">Criar regra</a></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-4 px-4 sm:px-6 lg:px-8"><x-flash-message /><div class="mv-surface overflow-hidden"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Regra</th><th class="px-4 py-3">Âmbito</th><th class="px-4 py-3">Tipologia</th><th class="px-4 py-3">Estado</th><th></th></tr></thead><tbody class="divide-y divide-ink-100">@forelse($rules as $rule)<tr><td class="px-4 py-3 font-semibold">{{ $rule->name }}</td><td class="px-4 py-3">{{ $rule->contest?->title ?? $rule->program?->name }}</td><td class="px-4 py-3">{{ $rule->typology ?? 'Qualquer' }}</td><td class="px-4 py-3">{{ $rule->is_active ? 'Ativa' : 'Inativa' }}</td><td class="px-4 py-3 text-right"><a class="font-semibold text-mvhab-primary" href="{{ route('backoffice.allocation.typology-rules.edit', $rule) }}">Editar</a></td></tr>@empty<tr><td colspan="5" class="px-4 py-8 text-center text-ink-500">Sem regras.</td></tr>@endforelse</tbody></table></div>{{ $rules->links() }}</div></div>
</x-app-layout>
