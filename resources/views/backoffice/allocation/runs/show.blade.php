<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-mvhab-primary">Atribuição</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $allocationRun->run_number }}</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-4 px-4 sm:px-6 lg:px-8"><x-flash-message />
        <div class="rounded-2xl border border-ink-100 bg-white p-6 text-sm"><dl class="grid gap-4 md:grid-cols-4"><div><dt class="text-ink-500">Concurso</dt><dd class="font-semibold">{{ $allocationRun->contest?->title }}</dd></div><div><dt class="text-ink-500">Método</dt><dd>{{ $allocationRun->allocation_method->label() }}</dd></div><div><dt class="text-ink-500">Estado</dt><dd>{{ $allocationRun->status->label() }}</dd></div><div><dt class="text-ink-500">Atribuições</dt><dd>{{ $allocationRun->total_allocations }}</dd></div></dl><div class="mt-6 flex flex-wrap gap-2"><form method="POST" action="{{ route('backoffice.allocation.runs.lock', $allocationRun) }}">@csrf<button class="rounded-2xl border border-mvhab-support/40 px-3 py-2 font-semibold text-mvhab-primary">Bloquear</button></form><form method="POST" action="{{ route('backoffice.allocation.runs.cancel', $allocationRun) }}">@csrf<button class="rounded-2xl border border-red-200 px-3 py-2 font-semibold text-red-700">Cancelar</button></form></div></div>
        <div class="mv-surface overflow-hidden"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Candidato</th><th class="px-4 py-3">Habitação</th><th class="px-4 py-3">Estado</th><th></th></tr></thead><tbody class="divide-y divide-ink-100">@forelse($allocationRun->allocations as $allocation)<tr><td class="px-4 py-3">{{ $allocation->candidate?->name }}</td><td class="px-4 py-3">{{ $allocation->housingUnit?->code }}</td><td class="px-4 py-3">{{ $allocation->status->label() }}</td><td class="px-4 py-3 text-right"><a class="font-semibold text-mvhab-primary" href="{{ route('backoffice.allocation.allocations.show', $allocation) }}">Abrir</a></td></tr>@empty<tr><td colspan="4" class="px-4 py-8 text-center text-ink-500">Sem atribuições.</td></tr>@endforelse</tbody></table></div>
    </div></div>
</x-app-layout>
