<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between gap-4"><div><p class="text-sm font-semibold text-mvhab-primary">Atribuição</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Execuções de atribuição</h1></div><a href="{{ route('backoffice.allocation.runs.create') }}" class="mv-button-primary">Nova execução</a></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-4 px-4 sm:px-6 lg:px-8"><x-flash-message /><div class="mv-surface overflow-hidden"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Número</th><th class="px-4 py-3">Concurso</th><th class="px-4 py-3">Método</th><th class="px-4 py-3">Estado</th><th></th></tr></thead><tbody class="divide-y divide-ink-100">@forelse($runs as $run)<tr><td class="px-4 py-3 font-semibold">{{ $run->run_number }}</td><td class="px-4 py-3">{{ $run->contest?->title }}</td><td class="px-4 py-3">{{ $run->allocation_method->label() }}</td><td class="px-4 py-3">{{ $run->status->label() }}</td><td class="px-4 py-3 text-right"><a class="font-semibold text-mvhab-primary" href="{{ route('backoffice.allocation.runs.show', $run) }}">Abrir</a></td></tr>@empty<tr><td colspan="5" class="px-4 py-8 text-center text-ink-500">Sem execuções.</td></tr>@endforelse</tbody></table></div>{{ $runs->links() }}</div></div>
</x-app-layout>
