<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Atribuição</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $allocationRuleSet->name }}</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-5xl space-y-4 px-4 sm:px-6 lg:px-8"><x-flash-message /><div class="rounded-md border border-ink-100 bg-white p-6 text-sm"><dl class="grid gap-4 md:grid-cols-2"><div><dt class="text-ink-500">Método</dt><dd class="font-semibold">{{ $allocationRuleSet->allocation_method->label() }}</dd></div><div><dt class="text-ink-500">Estado</dt><dd class="font-semibold">{{ $allocationRuleSet->status->label() }}</dd></div><div><dt class="text-ink-500">Aceitação</dt><dd>{{ $allocationRuleSet->requires_acceptance ? 'Obrigatória' : 'Dispensada' }}</dd></div><div><dt class="text-ink-500">Prazo</dt><dd>{{ $allocationRuleSet->acceptance_deadline_days }} dias</dd></div></dl><div class="mt-6 flex flex-wrap gap-2"><a href="{{ route('backoffice.allocation.rule-sets.edit', $allocationRuleSet) }}" class="rounded-md border border-ink-200 px-3 py-2 font-semibold">Editar</a><form method="POST" action="{{ route('backoffice.allocation.rule-sets.activate', $allocationRuleSet) }}">@csrf<button class="rounded-md border border-civic-200 px-3 py-2 font-semibold text-civic-800">Ativar</button></form><form method="POST" action="{{ route('backoffice.allocation.rule-sets.archive', $allocationRuleSet) }}">@csrf<button class="rounded-md border border-ink-200 px-3 py-2 font-semibold">Arquivar</button></form></div></div></div></div>
</x-app-layout>
