<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Atribuição</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Relatórios de atribuição</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-4 px-4 sm:px-6 lg:px-8"><x-flash-message /><form method="POST" action="{{ route('backoffice.allocation.reports.store') }}" class="flex flex-col gap-3 rounded-md border border-ink-100 bg-white p-4 md:flex-row">@csrf<select name="allocation_run_id" class="rounded-md border-ink-200 text-sm md:flex-1">@foreach($runs as $run)<option value="{{ $run->id }}">{{ $run->run_number }} · {{ $run->contest?->title }}</option>@endforeach</select><button class="rounded-md bg-civic-700 px-4 py-2 text-sm font-semibold text-white">Gerar relatório</button></form><div class="rounded-md border border-ink-100 bg-white"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Relatório</th><th class="px-4 py-3">Concurso</th><th class="px-4 py-3">Estado</th><th></th></tr></thead><tbody class="divide-y divide-ink-100">@forelse($reports as $report)<tr><td class="px-4 py-3 font-semibold">{{ $report->report_number }}</td><td class="px-4 py-3">{{ $report->contest?->title }}</td><td class="px-4 py-3">{{ $report->status->label() }}</td><td class="px-4 py-3 text-right"><a class="font-semibold text-civic-700" href="{{ route('backoffice.allocation.reports.show', $report) }}">Abrir</a></td></tr>@empty<tr><td colspan="4" class="px-4 py-8 text-center text-ink-500">Sem relatórios.</td></tr>@endforelse</tbody></table></div>{{ $reports->links() }}</div></div>
</x-app-layout>
