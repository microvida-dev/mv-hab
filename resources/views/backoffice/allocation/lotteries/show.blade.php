<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Atribuição</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Sorteio #{{ $lotteryRun->id }}</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-4 px-4 sm:px-6 lg:px-8"><x-flash-message /><div class="rounded-md border border-ink-100 bg-white p-6 text-sm"><dl class="grid gap-4 md:grid-cols-3"><div><dt class="text-ink-500">Concurso</dt><dd class="font-semibold">{{ $lotteryRun->contest?->title }}</dd></div><div><dt class="text-ink-500">Estado</dt><dd>{{ $lotteryRun->status->label() }}</dd></div><div><dt class="text-ink-500">Hash</dt><dd class="break-all">{{ $lotteryRun->audit_hash }}</dd></div></dl><div class="mt-6 flex gap-2"><a href="{{ route('backoffice.allocation.lotteries.audit', $lotteryRun) }}" class="rounded-md border border-ink-200 px-3 py-2 font-semibold">Auditoria</a><form method="POST" action="{{ route('backoffice.allocation.lotteries.lock', $lotteryRun) }}">@csrf<button class="rounded-md border border-civic-200 px-3 py-2 font-semibold text-civic-800">Bloquear</button></form></div></div><div class="rounded-md border border-ink-100 bg-white"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Ordem</th><th class="px-4 py-3">Candidato</th><th class="px-4 py-3">Resultado</th></tr></thead><tbody class="divide-y divide-ink-100">@foreach($lotteryRun->drawResults as $result)<tr><td class="px-4 py-3">{{ $result->draw_order }}</td><td class="px-4 py-3">{{ $result->candidate?->name }}</td><td class="px-4 py-3">{{ $result->result_type->label() }}</td></tr>@endforeach</tbody></table></div></div></div>
</x-app-layout>
