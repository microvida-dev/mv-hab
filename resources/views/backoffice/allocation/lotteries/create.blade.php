<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-mvhab-primary">Atribuição</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Novo sorteio</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-3xl space-y-4 px-4 sm:px-6 lg:px-8"><x-flash-message /><form method="POST" action="{{ route('backoffice.allocation.lotteries.store') }}" class="mv-surface space-y-4 p-6">@csrf<label class="block text-sm font-medium text-ink-700">Execução<select name="allocation_run_id" class="mv-input mt-1 w-full">@foreach($runs as $run)<option value="{{ $run->id }}">{{ $run->run_number }} · {{ $run->contest?->title }}</option>@endforeach</select></label><label class="block text-sm font-medium text-ink-700">Semente<input name="seed" class="mv-input mt-1 w-full"></label><label class="block text-sm font-medium text-ink-700">Notas<textarea name="notes" class="mv-input mt-1 w-full"></textarea></label><div class="flex justify-end"><button class="mv-button-primary">Executar sorteio</button></div></form></div></div>
</x-app-layout>
