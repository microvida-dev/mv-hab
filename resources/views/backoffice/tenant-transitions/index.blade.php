<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Inquilino</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Transições para inquilino</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-4 px-4 sm:px-6 lg:px-8"><x-flash-message /><form method="POST" action="{{ route('backoffice.tenant-transitions.run') }}" class="flex gap-2 rounded-md border border-ink-100 bg-white p-4">@csrf<select name="winner_registration_id" class="flex-1 rounded-md border-ink-200">@foreach($winners as $winner)<option value="{{ $winner->id }}">{{ $winner->candidate?->name }} — vencedor #{{ $winner->id }}</option>@endforeach</select><button class="rounded-md bg-civic-700 px-4 py-2 text-sm font-semibold text-white">Executar transição</button></form><div class="rounded-md border border-ink-100 bg-white"><table class="min-w-full divide-y divide-ink-100 text-sm"><thead class="bg-ink-50 text-left text-xs font-semibold uppercase text-ink-500"><tr><th class="px-4 py-3">Inquilino</th><th class="px-4 py-3">Estado</th><th class="px-4 py-3">Contrato</th><th class="px-4 py-3">Conta</th></tr></thead><tbody class="divide-y divide-ink-100">@foreach($transitions as $transition)<tr><td class="px-4 py-3">{{ $transition->tenant?->name }}</td><td class="px-4 py-3">{{ $transition->status->label() }}</td><td class="px-4 py-3">{{ $transition->lease_contract_id ?? 'Sem contrato ativo' }}</td><td class="px-4 py-3">{{ $transition->tenant_financial_account_id ?? 'Sem conta' }}</td></tr>@endforeach</tbody></table></div>{{ $transitions->links() }}</div></div>
</x-app-layout>
