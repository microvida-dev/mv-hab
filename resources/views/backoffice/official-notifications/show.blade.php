<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Notificação oficial</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $officialNotification?->subject ?? 'Criar notificação' }}</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8">@if($officialNotification)<div class="rounded-md border border-ink-100 bg-white p-6"><p class="text-sm text-ink-500">{{ $officialNotification->status->label() }} · {{ $officialNotification->channel->label() }}</p><p class="mt-4 whitespace-pre-line text-sm">{{ $officialNotification->body }}</p><p class="mt-4 text-xs text-ink-500">Sem envio real por email/SMS nesta sprint.</p></div>@else<form method="POST" action="{{ route('backoffice.official-notifications.store') }}" class="space-y-4 rounded-md border border-ink-100 bg-white p-6">@csrf<x-text-input name="user_id" placeholder="ID do utilizador" class="w-full" /><select name="notification_type" class="w-full rounded-md border-ink-200">@foreach($types as $value => $label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select><x-text-input name="subject" placeholder="Assunto" class="w-full" /><textarea name="body" class="w-full rounded-md border-ink-200" placeholder="Corpo"></textarea><x-primary-button>Criar</x-primary-button></form>@endif</div></div>
</x-app-layout>

