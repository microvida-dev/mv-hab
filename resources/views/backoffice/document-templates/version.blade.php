<x-app-layout><x-slot name="header"><div><p class="text-sm font-semibold text-mvhab-primary">{{ $documentTemplateVersion->template->name }}</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Versão {{ $documentTemplateVersion->version_number }}</h1></div></x-slot><div class="py-8"><div class="mx-auto max-w-4xl space-y-6 px-4 sm:px-6 lg:px-8"><article class="mv-surface p-8"><p class="text-sm text-ink-500">{{ $documentTemplateVersion->status->label() }}</p><h2 class="mt-3 text-xl font-semibold">{{ $documentTemplateVersion->title }}</h2><p class="mt-6 whitespace-pre-line text-sm leading-7">{{ $documentTemplateVersion->body }}</p></article><div class="flex gap-3">@can('approve', $documentTemplateVersion)<form method="POST" action="{{ route('backoffice.document-template-versions.approve', $documentTemplateVersion) }}">@csrf<x-primary-button>Aprovar</x-primary-button></form><form method="POST" action="{{ route('backoffice.document-template-versions.activate', $documentTemplateVersion) }}">@csrf<x-secondary-button>Ativar</x-secondary-button></form>@endcan</div></div></div></x-app-layout>
