<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-4"><div><p class="text-sm font-semibold text-civic-700">Contratação</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Regras de renda</h1></div>@can('create', \App\Models\RentRuleSet::class)<a href="{{ route('backoffice.contracts.rent-rule-sets.create') }}" class="mv-button-primary"><x-ui-icon name="plus" class="h-4 w-4" />Nova regra</a>@endcan</div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-5 px-4 sm:px-6 lg:px-8"><x-flash-message /><div class="mv-surface overflow-hidden"><table class="mv-table"><thead><tr><th>Nome</th><th>Contexto</th><th>Método</th><th>Taxa</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($ruleSets as $ruleSet)<tr><td class="font-semibold">{{ $ruleSet->name }}</td><td>{{ $ruleSet->contest?->title ?? $ruleSet->program?->name }}</td><td>{{ $ruleSet->calculation_method->label() }}</td><td>{{ $ruleSet->effort_rate_percentage ?? '-' }}%</td><td>{{ $ruleSet->status->label() }}</td><td class="text-right"><a class="font-semibold text-civic-700" href="{{ route('backoffice.contracts.rent-rule-sets.show', $ruleSet) }}">Abrir</a></td></tr>@empty<tr><td colspan="6" class="text-center text-ink-500">Sem regras configuradas.</td></tr>@endforelse</tbody></table><div class="border-t border-ink-100 px-4 py-3">{{ $ruleSets->links() }}</div></div></div></div>
</x-app-layout>
