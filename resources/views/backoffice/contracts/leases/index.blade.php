<x-app-layout><x-slot name="header"><div class="flex items-center justify-between gap-4"><div><p class="text-sm font-semibold text-civic-700">Contratação</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Contratos processuais</h1></div><a href="{{ route('backoffice.contracts.leases.create') }}" class="mv-button-primary">Novo contrato</a></div></x-slot><div class="py-8"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><x-flash-message /><div class="mv-surface overflow-hidden"><table class="mv-table"><thead><tr><th>Número</th><th>Candidato</th><th>Habitação</th><th>Renda</th><th>Caução</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($contracts as $contract)<tr><td class="font-semibold">{{ $contract->contract_number }}</td><td>{{ $contract->candidate?->name }}</td><td>{{ $contract->housingUnit?->code }}</td><td>{{ $contract->monthly_rent }}</td><td>{{ $contract->deposit?->amount ?? '-' }}</td><td>{{ $contract->status->label() }}</td><td class="text-right"><a class="font-semibold text-civic-700" href="{{ route('backoffice.contracts.leases.show', $contract) }}">Abrir</a></td></tr>@empty<tr><td colspan="7" class="text-center text-ink-500">Sem contratos processuais.</td></tr>@endforelse</tbody></table><div class="border-t border-ink-100 px-4 py-3">{{ $contracts->links() }}</div></div></div></div></x-app-layout>
