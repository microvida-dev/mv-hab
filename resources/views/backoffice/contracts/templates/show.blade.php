<x-app-layout><x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-4"><div><p class="text-sm font-semibold text-mvhab-primary">Minuta</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">{{ $contractTemplate->name }}</h1></div><div class="flex gap-2"><a href="{{ route('backoffice.contracts.templates.edit', $contractTemplate) }}" class="mv-button-secondary">Editar</a><form method="POST" action="{{ route('backoffice.contracts.templates.activate', $contractTemplate) }}">@csrf<button class="mv-button-primary">Ativar</button></form></div></div></x-slot><div class="py-8"><div class="mx-auto max-w-5xl space-y-5 px-4 sm:px-6 lg:px-8"><x-flash-message /><div class="mv-surface p-6 text-sm"><p class="text-ink-500">{{ $contractTemplate->status->label() }} · versão {{ $contractTemplate->version_number }}</p><pre class="mt-4 whitespace-pre-wrap rounded-2xl bg-ink-50 p-4 text-sm">{{ $contractTemplate->template_body }}</pre></div><div class="mv-surface p-6"><h2 class="font-semibold">Cláusulas associadas</h2><ul class="mt-3 list-disc pl-5 text-sm">@forelse($contractTemplate->clauses as $clause)<li>{{ $clause->code }} · {{ $clause->title }}</li>@empty<li>Sem cláusulas associadas.</li>@endforelse</ul></div></div></div></x-app-layout>
