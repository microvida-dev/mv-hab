<x-app-layout>
    <x-slot name="header"><div><p class="text-sm font-semibold text-civic-700">Histórico técnico</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Verificações de elegibilidade</h1></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-5 px-4 sm:px-6 lg:px-8">
        <form method="GET" class="mv-surface grid gap-4 p-5 sm:grid-cols-3"><select name="result" class="rounded-md border-ink-200"><option value="">Todos os resultados</option>@foreach($results as $value => $label)<option value="{{ $value }}" @selected(request('result') === $value)>{{ $label }}</option>@endforeach</select><select name="check_type" class="rounded-md border-ink-200"><option value="">Todos os tipos</option>@foreach($types as $value => $label)<option value="{{ $value }}" @selected(request('check_type') === $value)>{{ $label }}</option>@endforeach</select><button class="mv-button-secondary">Filtrar</button></form>
        <div class="mv-surface overflow-hidden"><div class="overflow-x-auto"><table class="mv-table"><thead><tr><th>Data</th><th>Contexto</th><th>Tipo</th><th>Resultado</th><th>Candidatura</th><th></th></tr></thead><tbody class="divide-y divide-ink-100">@forelse($checks as $check)<tr><td>{{ $check->executed_at?->format('d/m/Y H:i') }}</td><td>{{ $check->contest?->title ?? $check->program?->name ?? 'Sem regra' }}</td><td>{{ $check->check_type->label() }}</td><td class="font-semibold">{{ $check->result?->label() }}</td><td>{{ $check->application?->application_number ?? '—' }}</td><td class="text-right"><a href="{{ route('backoffice.eligibility.checks.show', $check) }}" class="font-semibold text-civic-700">Detalhe</a></td></tr>@empty<tr><td colspan="6" class="text-center text-ink-500">Sem verificações.</td></tr>@endforelse</tbody></table></div><div class="border-t border-ink-100 px-4 py-3">{{ $checks->links() }}</div></div>
    </div></div>
</x-app-layout>
