<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-4"><div><p class="text-sm font-semibold text-mvhab-primary">{{ $ruleSet->name }}</p><h1 class="mt-1 text-2xl font-semibold text-ink-900">Critérios de elegibilidade</h1></div>@can('create', [\App\Models\EligibilityCriterion::class, $ruleSet])<a href="{{ route('backoffice.eligibility.criteria.create', $ruleSet) }}" class="mv-button-primary"><x-ui-icon name="plus" class="h-4 w-4" />Novo critério</a>@endcan</div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-5 px-4 sm:px-6 lg:px-8"><x-flash-message /><div class="mv-surface overflow-hidden"><div class="overflow-x-auto"><table class="mv-table">
        <thead><tr><th>Ordem</th><th>Código / nome</th><th>Categoria</th><th>Operador</th><th>Obrigatório</th><th>Estado</th><th></th></tr></thead>
        <tbody class="divide-y divide-ink-100">@forelse($criteria as $criterion)<tr><td>{{ $criterion->sort_order }}</td><td><p class="font-semibold text-ink-900">{{ $criterion->name }}</p><p class="text-xs text-ink-500">{{ $criterion->code }}</p></td><td>{{ $criterion->category->label() }}</td><td>{{ $criterion->operator->label() }}</td><td>{{ $criterion->is_mandatory ? 'Sim' : 'Não' }}</td><td>{{ $criterion->is_active ? 'Ativo' : 'Inativo' }}</td><td class="text-right"><a href="{{ route('backoffice.eligibility.criteria.edit', $criterion) }}" class="font-semibold text-mvhab-primary">Editar</a></td></tr>@empty<tr><td colspan="7" class="text-center text-ink-500">Sem critérios configurados.</td></tr>@endforelse</tbody>
    </table></div><div class="border-t border-ink-100 px-4 py-3">{{ $criteria->links() }}</div></div></div></div>
</x-app-layout>
